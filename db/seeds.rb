

# groups = Group.all
# contacts = Contact.all

# contacts.each do |contact|
#   selected_groups = groups.sample(rand(2..4))

#   selected_groups.each do |group|
#     GroupContact.create(contact_id: contact.id, group_id: group.id)
#   end

# end
